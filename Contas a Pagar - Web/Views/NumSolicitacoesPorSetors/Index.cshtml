@model IEnumerable<Contas_a_Pagar___Web.Models.NumSolicitacoesPorSetor>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

@*<p>
    @Html.ActionLink("Create New", "Create")
</p>*@
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Setor)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NumSolicitacoes)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Setor)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NumSolicitacoes)
        </td>
        <td>
            @Html.ActionLink("Details", "Details", new { id=item.SetorID })
        </td>
    </tr>
}

</table>
