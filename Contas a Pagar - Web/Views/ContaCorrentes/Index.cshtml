@model IEnumerable<Contas_a_Pagar___Web.Models.ContaCorrente>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Limite)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Agencia1.Endereco)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PlanoContas.Descricao)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Limite)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Agencia1.Endereco)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PlanoContas.Descricao)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Conta }) |
            @Html.ActionLink("Details", "Details", new { id=item.Conta }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Conta })
        </td>
    </tr>
}

</table>
